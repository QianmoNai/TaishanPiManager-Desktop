# v2.25 验证记录

先复制 v2.24 至新版本目录，再完成修改；旧版未覆盖。

- 新增 HTTP/HTTPS Clash/Mihomo 配置链接导入，保留本地文件入口、核心校验和失败保留原配置的流程。
- 下载支持重定向、4 MB 限制、网络超时和错误信息脱敏；链接不保存，成功后清空。
- 卡片更新复用现有控件，测速时不自动重排；设备操作不再禁用当前焦点卡片，避免滚动区跳到其他控件。
- 新增 60 节点连续批量测试，逐轮断言滚动位置、控件身份、焦点和节点顺序保持稳定；验证下方节点点击后位置保持稳定。
- 本地 HTTP 服务器验证订阅下载、重定向、403 脱敏和大小限制；验证导入仍走核心校验且失败不会替换配置，运行中的服务禁止导入。
- 全量测试 83 项通过；1000px 深色订阅导入区域截图已检查。
- 使用虚构节点和订阅测试数据，未导入任何用户真实订阅，未改动设备配置，未同步或编译 WSL SDK。

- PyInstaller 打包成功，成品 --smoke-test 返回 0；SHA256.txt 已更新。
