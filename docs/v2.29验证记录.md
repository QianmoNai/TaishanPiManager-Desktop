# v2.29 验证记录

- 从 v2.28 创建新目录，旧版本保留。
- 新增“串口助手”插件，默认 UART3：排针 8 = UART3_TX_M1 / GPIO3_B7，10 = UART3_RX_M1 / GPIO3_C0，6 = GND；明确 3.3V TTL、交叉连接和共地要求。
- 串口参数：1200～2000000 波特率、5～8 数据位、无/奇/偶校验、1/2 停止位、无/RTS-CTS/XON-XOFF 流控；UART3 排针默认无流控。
- 支持文本/HEX 接收显示、发送、LF/CRLF/CR、定时发送、日志保存；接收缓冲与发送长度均有限制。
- 支持 FireWater CSV 和 JustFloat 小端 float32 波形解析，最多 8 通道、保留 1000 帧、CSV 导出。
- 刷新显示串口占用、系统保留状态、设备树 UART 和当前 GPIO 复用；拒绝抢占占用/保留串口。
- “导出 SDK 配置”生成经过当前设备树符号验证的 .dtsi 片段，必须由用户审查冲突并手动编译；不在线修改 GPIO 或固件。
- ADB 通过板端 Perl 临时会话工作，打开时应用 termios，关闭/断连恢复原参数；不保存设备数据。
- 实机串口3伪终端回环验证：二进制 RX/TX、关闭后 termios 恢复、清理成功。
- 全量测试与界面检查见最终输出；未同步敏感信息，未运行 WSL SDK 编译。

- 全量 102 项测试通过。PyInstaller 打包完成，成品启动自检退出码：0。SHA256.txt 已更新。
