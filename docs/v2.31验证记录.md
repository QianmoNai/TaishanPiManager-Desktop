# v2.31 验证记录

- 从 v2.30 创建新版本目录，旧版保留。
- 修复连接第二个 Wi‑Fi 后覆盖单网络配置的问题：保存完整 `/etc/wpa_supplicant.conf`，同步备份到 `/userdata/etc/tspi-wifi.conf`。
- 连接新 Wi‑Fi 时先立即选择新 profile；认证完成后启用全部已有 profile，再保存配置，断开当前网络后可自动回连其他网络。
- 修复旧版本可能留下的 `[DISABLED]` profile：连接流程会启用所有已保存 profile。
- 实机检查曾发现 2 个 profile，其中 1 个为 disabled；已在设备上启用并保存，检查结果为当前 profile + 其他 enabled profile。未输出 SSID 或密码。
- 全量测试 105 项通过，覆盖 profile 保留、全部 profile 重启用、选择新网络后保存顺序、密码不落日志和设备断连处理。
- 未同步敏感信息，未执行 WSL SDK/固件编译。

- 全量 105 项测试通过。PyInstaller 打包完成，成品启动自检退出码：0。SHA256.txt 已更新。
