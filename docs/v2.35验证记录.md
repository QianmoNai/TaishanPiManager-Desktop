# v2.35 验证记录

- 在“引脚助手”中用用户提供的泰山派 40 针排针引脚图替换原“引脚复用与资源摘要”卡片。
- 图片随桌面程序打包，并按窗口宽度保持比例缩放。
- 图中保留 GPIO、I2C、SPI、PWM、UART3、3.3V、5V、GND 等排针复用信息。
- GPIO、I2C、SPI、PWM 的原有功能保持不变。
- 自动化测试：116 项通过。
- 冒烟测试：打包后 `--smoke-test` 返回 0。
