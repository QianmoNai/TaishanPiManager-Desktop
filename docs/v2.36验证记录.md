# v2.36 验证记录

- GPIO 操作改为按用户提供的 40 针排针图选择物理脚位。
- GPIO 下拉框显示：排针脚号、GPIO 名称、Linux 全局编号和复用功能。
- 5V、3.3V、GND 等供电脚不会出现在 GPIO 操作列表中。
- UART3 排针 8/10 脚分别映射为 GPIO3_B7/GPIO3_C0，对应 Linux GPIO 111/112。
- 后端 GPIO、I2C、SPI、PWM 功能保持不变。
- 自动化测试：117 项通过。
- 未修改设备树，未执行固件编译。
