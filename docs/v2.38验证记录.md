# v2.38 验证记录

- 修复无线网卡选择：优先使用 wlan0，并过滤没有 wpa_supplicant 控制接口的辅助 wlan1。
- 连接前取消可能卡住的扫描，重新选择配置后再等待认证和 DHCP。
- 改进超时提示，明确要求 USB ADB、wlan0 和重新扫描。
- 修复开机自动连接：开机脚本在控制接口存在时也会执行 `enable_network all`、`reconnect` 和 DHCP 刷新，不再直接退出；系统配置缺失时从 `/userdata/etc/tspi-wifi.conf` 恢复。
- 未输出 Wi-Fi 密码，未将敏感配置同步到 WSL/SDK。
- 自动化测试：118 项通过。
- 未修改设备树，未执行 WSL SDK 编译。

- 修复连接脚本中未展开的 Python 占位文字，正确写入开机 helper 和 init 脚本。
- 开放网络、WPA/WPA2 两种完整连接脚本使用虚构测试凭据，均通过板端 `sh -n`（只校验、不执行连接）。
- 实机当前 wlan0 为 SCANNING，无 IPv4 和默认路由。此前扫描未发现匹配的已保存热点；尚未验证成功认证、断电重连及 USB 网卡插拔差异。
- 最终桌面 EXE 已重新打包；冒烟测试退出码 0；SHA256.txt 已更新。
- 原 TaishanPiManager.exe 被运行中的进程占用，未替换成功。最终修复包为 TaishanPiManager-v2.38.exe，请使用此文件。
