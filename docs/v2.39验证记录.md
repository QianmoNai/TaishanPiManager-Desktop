# v2.39 验证记录

- 从 v2.38 创建独立版本目录后修改。
- 新增 Windows DPAPI 当前用户加密的本地多 Wi-Fi 配置、删除、自动填充和批量合并同步。
- 120 项测试通过，包含真实 DPAPI 加密往返、更新/删除、损坏文件保护、模拟板端合并及保留其他网络。
- 批量同步完整脚本通过真实板端 sh -n；未将虚构测试网络实际写入板端。
- 深色模式页面已截图检查。
- 当前实机 QWRT 已认证，IPv4 为 192.168.1.148，运行配置同时包含 iQOO。
- 尚未使用真实密码完成新版批量同步或断电重连验证；用户需要在新版保存密码。
- 未同步敏感信息到 SDK/WSL，未编译固件。
- 实机确认并修复持久化路径问题：wpa_cli save_config 将运行目录的配置符号链接替换为普通文件，原实现备份了未更新的 /etc 配置。现在从进程 -c 参数获取真实配置路径。
- 已在板端执行修复后的持久化流程，/etc/wpa_supplicant.conf 与 /userdata/etc/tspi-wifi.conf 均包含 iQOO 和 QWRT，未输出密码。
- 修复后重新运行全部 120 项测试通过。
- 第一轮打包 EXE 启动时访问被拒绝，之后根目录文件消失；本机有 360，尚待确认拦截记录。最终 EXE 启动验证未完成。
