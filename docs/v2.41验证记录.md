# v2.41 验证记录

- 从 v2.40 复制新版本目录后修改，旧版本保留。
- 修复 PinPanel.render 状态栏使用已删除变量 chips 的 NameError，改为统计 available_pwm。
- 增加含 PWM 和空资源结果的页面渲染回归测试。
- 全部 123 项测试通过。
- 未操作板端 GPIO、I2C、SPI、PWM 硬件，未修改 WSL/SDK。
- EXE 已打包，但启动检查被 Windows 拒绝访问（WinError 5），未完成 EXE 冒烟验证。
