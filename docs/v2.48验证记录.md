# v2.48 UP自建测速节点

服务器：192.161.162.155:8080，systemd tspi-speedtest，已开机自启。专用 HTTP 接口兼容现有板端 Perl 测速。最多8个并发连接，单次上传最大4MiB且不落盘，下载16MiB，无认证的公网测速端点，使用会消耗服务器流量。未改动80/443服务。

板端 eth1 实测：下载52.57 Mbps，上传16.57 Mbps，HTTP响应延迟164ms。短时单连接结果，仅表示该次泰山派到节点的链路表现。详见 UP节点实测.json。
