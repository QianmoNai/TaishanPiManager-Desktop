# v2.63 · 检查更新

修改前基线标签：`v2.63`。此标签不包含本次功能；程序运行版本为 `2.63`，当前只完成本地构建，尚未发布新的正式 Release。

## 功能范围

- 侧栏手动检查 GitHub 最新正式 Release，独立异步网络请求，不占用 ADB 任务队列。
- 数字版本比较，识别 `v2.64`、`v2.64.1`、`v2.64-release`；忽略草稿和预发布，普通基线标签不是更新依据。
- 更新说明以纯文本显示，浏览器目标限制在项目固定发布地址；不自动下载、覆盖安装或执行文件。
- 超时、限流、HTTP/TLS 错误、无效 JSON、超大响应均作为失败处理。关闭对话框或主窗口时取消更新请求。
- 当前版本由 `source/app_updates.py` 的 `APP_VERSION` 提供，修正侧栏旧的 v2.48 标识。

## 已执行的验证

36 项测试通过：`test_app_updates`、`test_desktop`、`test_plugin_motion`、`test_external_plugin_ui`、`test_external_plugins`。覆盖新版本/相同/较旧版本、恶意 URL 不被采用、重复点击、异常状态、响应大小限制、超时后重试、关闭取消、主窗口单例入口和关闭联动，以及已有桌面和插件回归。

浅色/深色更新对话框离屏渲染已检查。匿名访问真实 GitHub 更新接口当时返回 HTTP 404，程序正确显示查询失败；没有将它当成“已经是最新版”。该结果不证明仓库具体为何不可访问，也未通过新增凭据或关闭 TLS 校验规避错误。实际新版本成功响应通过模拟返回验证。

PyInstaller 构建至 `dist/v2.63`。精简 Windows x64 ZIP 仅包含 EXE、adb、iperf3 和 licenses；解压后的 `--smoke-test` 退出码 0。

ZIP 文件：`TaishanPiManager-v2.63-Windows-x64.zip`，74,044,865 字节。SHA-256：

```text
51079F7EC70D9D743AE91A5C2133ACB7B3CE55C255E704353E3C7DC17129C46A
```

本次未连接开发板做新的硬件实测，未执行全仓库所有测试，也未发布或推送远程仓库。
