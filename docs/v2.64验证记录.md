# v2.64 · Gitee 更新检测

修改前基线标签：`v2.64`。当前程序版本为 `2.64`，新功能尚未发布 Release。

- 更新接口切换到 Gitee API v5 的本项目最新发行版接口，发布页按钮也切换到 Gitee，不回退 GitHub。
- 请求使用 `Accept: application/json`，不读取登录凭据或发送 Authorization。
- 兼容 Gitee 正式发行版缺少 `draft` 字段的情况；显式草稿、预发布、异常版本仍拒绝。
- 数字版本比较、异步查询、超时、大小上限、纯文本说明、关闭取消和手动下载方式保留。

38 项测试通过：更新检查、桌面、插件动画、插件操作界面和包校验。新增验证固定 Gitee 接口、请求头和缺少 draft 字段的响应。浅色/深色界面通过模拟 Gitee 更新信息离屏渲染检查；截图中的 v2.65-release 是模拟数据，不表示线上已发布。

本轮未验证真实 Gitee API 成功响应，不宣称在线更新已经端到端通过。用户需在 Gitee 创建可公开访问的正式发行版，不能仅推送基线标签。

运行包输出到 `dist/v2.64`；另提供仅含 EXE、adb、iperf3 和 licenses 的 Windows x64 便携 ZIP。本次没有新的板端硬件实测，也未推送远程或发布 Release。

精简 ZIP 解压后的启动自检退出码 0。ZIP 大小为 74,045,452 字节，SHA-256：

```text
6681A123BA8E61D2E9DD4126DA12C9CF0911494EC6B09919D99CC79653E24DFC
```
