# v2.65 更新诊断与安全跳转验证

日期：2026-09-24。本地打包，未推送或发布 Release。`v2.65` 是修改前基线标签，不是修改后的发布源码。

## 变更

- 失败时显示 HTTP 状态、Qt 错误枚举、固定接口地址及针对性提示，不显示任意服务器正文或潜在凭据。
- 支持最多三次重定向，仅允许 HTTPS、gitee.com、443 端口及本仓库发行版 API 路径；拒绝用户信息、查询参数和片段。
- 保留 TLS 校验、512 KiB 响应限制、15 秒总超时及关闭窗口时取消请求。

## 验证

- `python -B -m unittest test_app_updates test_desktop test_plugin_motion test_external_plugin_ui test_external_plugins`：41 项通过。
- 包含安全跳转、跳转次数上限、不安全跳转拒绝和 TLS 失败诊断的模拟测试。
- PyInstaller 构建成功：`dist/v2.65/TaishanPiManager.exe`。
- 最简 ZIP 仅含 EXE、adb、iperf3、licenses；解压后 `--smoke-test` 退出码为 0。
- ZIP 位于仓库外 `../TaishanPiManager-release/v2.65-local/TaishanPiManager-v2.65-Windows-x64.zip`，大小 74044090 字节。
- ZIP SHA256：`78A8214012E66F36C9C401F44CD5361BEE3DA5D18619AB95BD857A80DE58BA85`。

## 限制与下一步

Gitee 在线访问受当前环境安全检查限制，本次未绕过限制，也未验证真实 API 请求成功。上述测试不能证明用户网络故障已消除；请运行新版，点击“检查更新”，若仍失败，反馈说明框中的 HTTP 状态和 Qt 网络状态，以确定后续修复方向。没有进行新的开发板实机测试。
