# v2.6 插件中心验证

- 真实 USB 设备读取插件状态：已安装、已停止，与板端状态一致。
- 验证搜索、空搜索结果、网络分类、已安装筛选、详情进入/返回与断开设备状态清除。
- 详情页读取板端停止状态，显示空速度值，无伪造流量。
- 浅色 / 深色及 1000×740 小窗口离屏渲染已检查。
- 保留安装/升级、服务启停、Wi-Fi、终端、文件、日志、流量和测速入口。
- 本次没有更改设备端服务状态，没有触发测速或修改防火墙。

- 38 项回归测试通过。
- 最终 EXE 离屏启动、插件目录与附带组件检查通过，正常退出（代码 0）。
