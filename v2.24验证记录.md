# v2.24 验证记录

基于 v2.23 新建版本目录后修改，旧版保留。

## 修改

网络代理插件改为响应式节点卡片；点击切换、高亮当前节点、协议/UDP 和延迟状态、搜索与排序；增加单节点及当前组批量测速，支持取消。服务配置管理默认折叠（缺少核心/配置时展开）。适配浅色/深色与 1000px 小窗口。

## 验证

- 全量 Python/Qt 测试：77 项通过。
- 新增节点快照、测速 URL 编码、地址校验、四节点并发、成功/失败/拦截策略区分、控制错误中止测试等检查。
- 本地 HTTP 控制接口测试覆盖 200/503/401 响应及临时 ADB 转发清理，使用虚构密钥。
- Qt 检查卡片点击成功后才更新选中状态、测速按钮不会切换节点、搜索排序、自动组禁止手动切换、取消与设备重置丢弃过期结果。
- 浅色/深色 1200px 为三列，深色 1000px 为两列；截图使用虚构节点和示例延迟，不代表真实测量结果。
- 实机限制：ADB 仅发现 offline 网络设备，无可用 USB 设备，未测量用户真实节点，也未修改设备配置。
- 未同步敏感信息到 SDK；未运行 WSL SDK/固件编译。

桌面可执行文件的打包、启动自检与校验值见本目录产物及 SHA256.txt。

- 最终 PyInstaller 打包成功；根目录 EXE --smoke-test 退出码 0。SHA256.txt 已更新。
