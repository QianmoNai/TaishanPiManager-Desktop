# v2.34 验证记录

- 新增插件：引脚助手。
- 覆盖：GPIO 读取/方向与电平操作、I2C 扫描、SPI 设备节点查看、PWM 状态/配置、pinmux 摘要。
- 安全：GPIO 输出/写入和 PWM 配置需要桌面端确认；GPIO、I2C、PWM 参数严格限制格式和范围；不执行任意 SPI 写入和 I2C 寄存器写入。
- 设备树与 SDK：仅提供资源查看和说明，不在线修改设备树；WSL SDK 编译由用户手动完成。
- 敏感信息：未打包 Wi-Fi 密码、代理令牌、订阅地址、真实设备地址。
- 自动化测试：115 项通过。
- 冒烟测试：打包后 `--smoke-test` 应返回 0。
