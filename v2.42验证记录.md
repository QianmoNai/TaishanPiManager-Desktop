# v2.42 验证记录

- 从 v2.41 创建新版本目录。
- 插件中心新增可选 LD06 雷达视图，支持离线下载 ZIP、校验安装、升级、卸载和状态检测。
- 使用 UART3 `/dev/ttyS3` 230400 8N1，加入 CRC8 校验、断帧重组、坏帧统计、量程/信号过滤和点云 CSV 导出。
- 128 项自动化测试通过。
- `ld06-helper.pl` 已上传到板端临时路径并通过 `perl -c`，临时文件已删除，未安装或启动服务。
- 未强制停止现有 LD06/LCD 监控；运行时检测 UART3 占用。
- SDK/WSL 未修改，未执行固件编译。
- EXE 已生成；启动冒烟测试被 Windows 拒绝访问（WinError 5），可能受本机安全软件拦截。
