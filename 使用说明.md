# 泰山派可视化管理台 v1.0

双击 `TaishanPiManager.exe`，软件会启动本机服务并自动打开浏览器页面。不需要安装 Python、Node.js，也不需要在泰山派安装额外管理服务。请将 EXE 与 `adb` 文件夹保存在一起。

## USB 连接

1. 启动泰山派，使用支持数据传输的 USB 线连接开发板的 OTG/调试接口。
2. 点击「刷新设备」，选择在线设备。
3. 如果设备列表为空，请检查 USB 数据线、板端 adbd 服务和 Windows ADB 驱动。Loader/Maskrom 烧录模式不是 ADB 模式。
4. 如果显示 unauthorized，在支持授权确认的系统上允许 USB 调试。Buildroot 固件一般不显示 Android 授权弹窗。

## 功能

- 设备概览：CPU 使用率、内存、温度、运行时间、磁盘和网络。CPU 需两次采样，约 5 秒后显示。
- 文件管理：浏览目录、上传、下载；每个文件最多 512 MB。上传同名文件前会确认覆盖。暂不提供删除、递归下载或固件刷写。
- 系统日志：内核、系统 messages、已安装的网络健康监控日志，支持保存到电脑。
- 命令控制台：单次 Shell 命令，最长等待 30 秒，输出最多 2 MB。每次独立会话，`cd` 不会保留到下一条命令。超时只终止本地等待，远端程序可能仍在运行，勿反复启动长任务。
- 服务与维护：调用已有的 `/userdata/bin/status_check_monitor`、`start_check_monitor`、`stop_check_monitor`；支持确认后重启。新固件未装这些脚本时会提示不存在。
- 网络 ADB：展开「网络连接」，填写设备地址，例如 `192.168.1.150:5555`。设备必须先启用网络 ADB。

## 启动与退出

默认页面为 `http://127.0.0.1:18766`；端口占用时自动选择其他端口并打开。服务仅监听本机，不向局域网开放。页面操作使用随机会话令牌，并验证请求来源。

关闭浏览器标签页不会退出后台服务；点击页面左下角「退出管理软件」完成退出。若浏览器没有自动打开，可从命令行运行 `TaishanPiManager.exe --port 18767` 后访问对应地址。程序不自动刷机、重启或修改开发板配置；命令与上传由用户发起。

USB 和网络设备可以共存，操作始终使用所选设备的序列号。不要同时通过两个连接对同一台板执行写操作。

## 开发与验证

源码位于 `source`，仅依赖 Python 标准库：

```powershell
python source/app.py
python -m unittest discover -s source -p test_app.py -v
```

通过环境变量 `TAISHAN_ADB` 或参数 `--adb` 指定自定义 adb.exe。此软件附带本机已有的 Android platform-tools ADB 组件，许可证见 `adb/NOTICE.txt`。ADB 驱动需要由 Windows/开发板厂商提供。

该版本完成模拟设备接口测试和未连接设备场景验证。交付时泰山派未连接，真实硬件状态、文件传输与服务控制仍需连接后验证。自动浏览器视觉检查因工具安全策略验证不可用而未完成；页面包含适配窄窗口的样式。
