# 泰山派设备管理 · 原生桌面版 v2.3

新增“服务与维护 → 安装监控插件”。保留 v2.2.1 的 ADB 实机兼容修复；设备概览、日志、文件传输、命令、Wi-Fi 扫描与密码连接均已验证。

## 安装监控插件

1. 连接并选择泰山派，进入“服务与维护”。
2. 点击“安装监控插件”，在确认窗口核对目标设备和安装内容。
3. 需要开机自启时，先勾选“安装时启用开机自动启动”；默认不更改已有开机设置。
4. 安装完成后点击“启动服务”；用“查看状态”读取进程状态和最近日志，用“停止服务”结束后台监控。

安装包自带完整监控源码，板端不需要下载或修改 SDK。安装要求 root ADB、可写 /userdata，以及 Perl 和相关标准模块（安装前执行 perl -c 验证）。本机开发板已通过测试。

监控每 600 秒检查 QWRT 192.168.1.1 与香橙派 RAG/NAS 192.168.1.158，包括网络、SSH、Web、路由器 DNS 和外网。日志路径 /userdata/log/check_lan_monitor.log，每轮前超过 1 MB 时保留一份轮转日志。此版本不启用 LCD 或 LD06 雷达，不占用串口。若目标地址或服务变化，需要调整 /userdata/bin/check_orangepi.pl 中对应地址；出现 ORANGEPI_FAIL 等结果表示目标服务不可达，不表示安装失败。

安装前会校验上传文件 SHA256 和板端脚本语法；覆盖前备份到 /userdata/monitor-backups/随机编号。安装出错会尝试回滚，断电或 ADB 断开后的备份也会保留。只自动升级本安装器管理的文件，发现其他来源的旧脚本会停止，避免覆盖你的自定义版本。运行中的插件须先停止再升级。勾选开机启动时写入 /etc/init.d/S95check-monitor；未勾选不会删除已有自启配置。开机启动脚本已做语法检查，本次未重启开发板验证。

当前设备已实测安装和服务启停，测试后保持停止、未启用开机自启。直接点击“启动服务”即可继续使用。

双击 `TaishanPiManager.exe` 即可打开独立窗口。无需 Python、浏览器或 WebView，也不启动 HTTP 服务。请保留同目录下的 `adb` 文件夹。

## 新界面

iOS 风格的浅色界面、圆角卡片、蓝色强调色与侧边导航。支持窗口缩放、高 DPI、原生文件选择框；关闭窗口即可退出程序。正在传输或执行命令时会提示等待操作结束。

侧栏底部点击「深色模式」即可切换，再点击「浅色模式」可恢复。六个页面同步切换，Windows 标题栏随应用调整；系统原生文件选择框的外观由 Windows 决定。外观偏好自动保存在当前 Windows 用户设置中，下次启动会恢复；切换不会重连设备或清除输入。

## USB 使用

1. 用可传输数据的 USB 线连接开发板的 OTG / 调试接口。
2. 点击「刷新设备」，选择在线设备。
3. 若未发现设备，请检查数据线、Windows ADB 驱动和板端 adbd。Loader / Maskrom 烧录模式不是 ADB 模式。

网络连接需要设备已开启网络 ADB；点击「网络连接」，填写 IP 和端口。

## Wi-Fi 扫描与连接

1. 通过 USB 连接泰山派，在软件顶部选中设备。
2. 打开左侧「Wi-Fi 网络」，点击「扫描附近 Wi-Fi」。扫描发生在泰山派上；首次使用会自动选择无线网卡，发现多个网卡后可切换。
3. 在列表中选择网络，输入密码，点击「连接 Wi-Fi」。开放网络无需密码。
4. 等待认证和 DHCP 地址分配，约 1 分钟以内。成功后显示网络名称和 IPv4；若仅完成认证，会明确提示尚未获取 IPv4，可点击「刷新状态」。

支持当前 SDK 中 wpa_supplicant / wpa_cli 的 WPA/WPA2 个人网络（包括允许 WPA2 的 WPA2/WPA3 混合网络）和开放网络。隐藏网络、企业 EAP、WEP、纯 WPA3 暂不支持。检测到 NetworkManager、ConnMan 或 iwd 时会提示不兼容，不接管其配置。

扫描显示名称、信号强度、频段和加密类型；同名接入点可重复出现，悬停名称可查看 BSSID，连接由无线服务选择同名网络的接入点。没有网络时显示空列表。若已有 wpa_supplicant 无可用控制接口，会提示修复 ctrl_interface，不会终止已有无线进程。

连接使用新的临时网络配置；认证失败会尝试删除本次新增配置并恢复原连接。成功后保持本次选择，不删除其他已有网络。**当前版本不保存开机自动连接配置**，重启后的网络由设备原有配置决定。

密码默认隐藏，点击连接后清空，不保存到电脑或日志；WPA/WPA2 在电脑端派生 PSK 后通过 ADB 标准输入传递，SSID 使用十六进制避免 Shell 解释。派生密钥仅供板端无线服务在内存中使用。建议通过 USB 操作，网络 ADB 在切换 Wi-Fi 时可能断开，需要 USB 或新地址重新连接。

v2.2.1 已实机验证无线扫描、WPA2 密码认证、DHCP 和公网连通性。`Wi-Fi示例-*.png` 使用模拟数据；`实机-*.png` 使用真实设备数据。实际软件不会生成虚拟网络。

## 功能

- 概览：CPU、内存、温度、运行时间、存储与网络。CPU 第一次连接后需要约 5 秒完成两次采样。
- 文件：浏览目录，双击文件夹进入；上传/下载最大 512 MB，上传同名文件前确认。下载失败不会覆盖已有本地文件。暂不支持目录下载、删除或烧录固件。
- 日志：读取最近 250 行内核/系统/网络监控日志，可另存为本地文件。
- 命令：每条命令独立会话，最长等待 30 秒；勾选确认后执行。不要用于持续运行的编译或交互命令。超时后远端程序可能仍在运行。
- 维护：安装监控插件、查看状态、启动/停止服务，可选择配置开机自启；确认后重启设备。

页面中的设备状态均来自实际 ADB；软件不会使用模拟数据。“安装插件-*.png”为真实安装结果截图；“Wi-Fi示例-*.png”为标注的模拟展示。

## 源码与构建

`source/desktop.py` 是 Qt Widgets 原生界面，`source/adb_core.py` 是 ADB 通信逻辑。旧网页版本保留在原目录，本目录是全新的桌面版。

开发需要 Python 3.13、PySide6 6.11.2 和 PyInstaller 6.22.3。安装依赖后：

```powershell
python source/desktop.py
python -m unittest discover -s source -p test_*.py -v
python -m PyInstaller --noconfirm source/build_desktop.spec
```

将 `adb` 文件夹放在生成的 EXE 旁。可通过 `TAISHAN_ADB` 环境变量指定其他 ADB 路径。使用附带的打包配置可避免 PATH 中其他软件的 ICU 库覆盖 Windows 系统库；运行环境为 Windows 10/11 64 位。

此版本通过原生窗口测试、离屏视觉检查及 USB 实机插件安装、重装、监控运行和停止验证。详情见“插件验证记录.md”。
