# 泰山派设备管理 · 原生桌面版 v2.10

## 卸载插件

进入“插件中心 → 网络流量”，点击详情顶部红色“卸载插件”，核对设备后确认。

- 自动停止监控，移除插件程序及开机启动。
- 累计流量（/userdata/traffic-monitor/totals.json）、日志、原有备份保留；重新安装后可继续统计。
- 卸载前备份插件程序到 /userdata/monitor-backups/uninstall-随机编号。
- 仅处理本软件管理的固定文件；遇到来源不明的同名文件或符号链接会停止，避免误删。
- 文件移除失败时尝试恢复备份，服务可能已停止，需重新查看状态。
- 测速进行中不能卸载。取消确认不会发送卸载命令。
- 卸载成功后卡片变为“未安装”，可重新安装。内置工具无需安装，也不提供卸载。

本次只添加和验证卸载功能，未实际卸载当前泰山派上的插件。

 
## 插件中心

左侧“插件中心”采用卡片目录布局，提供搜索以及“全部 / 已安装 / 网络 / 系统”分类。当前包含 1 款可安装的板端插件（网络流量）与 4 项内置工具（Wi-Fi、终端、文件、日志）；这是随软件提供的本地目录，不是在线下载商店。

连接设备后进入插件中心会读取安装与运行状态，也可点击“刷新插件状态”。插件卡片显示未安装、已安装已停止、运行中或可更新；切换设备后会清除上一设备状态，检测失败不会继续显示旧的运行状态。“已安装”同时包括内置工具，卡片会明确标注来源。

点击网络流量卡片的“打开 / 查看详情”，进入独立详情页。顶部根据实际状态提供安装、升级、启动或服务管理入口；“管理”可直接定位到安装选项、服务启停与运行输出。点击“返回插件中心”回到目录。流量图表、测速与累计统计保留原功能。

内置工具卡片的“打开”会跳转到对应页面，无需安装。设备重启操作放在插件目录底部的“设备维护”卡片，仍需确认。浅色与深色模式均适用。

本次仅更新桌面界面和只读状态识别，没有重装板端插件、启动或停止服务。验证时板端插件已安装但处于停止状态。


## 交互终端

选择在线设备，打开左侧“终端”，点击“连接终端”，在深色终端区域输入命令，按 Enter 执行。

- 持续 ADB PTY 会话，支持 cd、环境变量、连续命令、实时输出，取消原来每条命令 30 秒的限制。
- 上下键读取板端 Shell 历史，Tab 补全；Ctrl+C 或下方“中断”按钮打断前台命令；支持 Ctrl+D、方向键、Home/End、PageUp/PageDown、F1–F12。
- 鼠标拖选后 Ctrl+Shift+C 复制；Ctrl+Shift+V 或“粘贴”按钮粘贴。多行粘贴会确认，以免意外执行命令；单次最多 64 KB。
- 滚轮或 Shift+PageUp/PageDown 查看最多 2000 行历史。“清空显示”只清理本地显示，不停止进程；要让板端重画屏幕，可按 Ctrl+L。
- 窗口大小会同步到远端 PTY，支持常见 ANSI 颜色、光标控制与备用屏幕。已实测 top；不承诺完整 xterm 扩展、终端鼠标协议或所有全屏程序兼容性。
- 终端持续连接与页面切换独立。切换设备、关闭软件或点击“断开”会提示结束会话；显式启动的后台任务可能继续运行，应按需在板端停止。
- 仅设置当前会话的 TERM、尺寸和 Bash 输入兼容选项，不修改 Shell 启动文件。此 Buildroot 没有 UTF-8 locale，已兼容中文输入输出，但很长的中文编辑行仍可能受板端 Readline 字符宽度计算影响。

终端输入输出不会由软件自动保存到日志文件；板端 Shell 自身的历史记录策略仍由设备决定。连接时使用的权限就是设备 ADB Shell 权限（当前泰山派为 root）。

## 网络流量与测速

“插件中心 → 网络流量”顶部显示所选网卡的实时下载速度、上传速度、累计下载和累计上传，支持网卡切换及最近 120 秒趋势图。每 2 秒更新；离开页面暂停电脑轮询，板端仍继续统计。断开设备、服务停止或数据过期时显示“—”，不会把旧值当成当前速度。

累计流量从首次启用统计开始，按网卡分别计算，包含局域网、互联网、网络 ADB 和测速数据，不是运营商账单流量。各网卡不相加，避免网桥重复统计。USB ADB 不算 Wi-Fi 流量。单位为 B、KiB、MiB、GiB；测速单位为 Mbps。

累计数据保存在 /userdata/traffic-monitor/totals.json，每 60 秒原子保存，正常停止也保存，设备重启后继续累计。突然断电可能丢失最近约 60 秒。重启或计数器归零后重新建立速度基线，避免虚假尖峰。

测速有两种模式：

- 与本电脑测速（局域网）：使用随包 iperf3，电脑主动连接泰山派指定网卡的临时测速端口。上传和下载各 5 秒，结果始终以泰山派为视角。电脑与泰山派须能在局域网中互通，仅 USB 连接而未联网时无法测速。
- 公网节点测速：内置上海、苏州、昆山、香港、新加坡、东京、法兰克福、伦敦、纽约和旧金山等固定 HTTP 节点。泰山派先探测延迟，再按延迟顺序依次尝试；某个节点失败会自动切换下一个，下载和上传结果始终来自同一个成功节点。公网节点受跨境链路、运营商策略和节点负载影响，结果仅代表泰山派到该节点的路径。
- 指定 iperf3 服务器：输入服务器 IPv4 / 域名和端口（默认 5201），对方需运行 iperf3 -s。测试由泰山派发起，填写公网服务器可测该路径的互联网速度。

局域网测速不代表宽带速度。测速占用带宽并产生流量，开始前会确认；测速期间图表继续刷新。临时测速服务结束时关闭，即使电脑断连也由板端 timeout 在约 20 秒内结束。不修改防火墙或 Wi-Fi 配置。失败显示错误，不显示假的成功结果。

## 安装 / 升级流量插件

1. 连接并选择泰山派，进入“插件中心 → 网络流量”，点击顶部“管理”进入“安装与服务管理”。
2. 若旧监控正在运行，先点击“停止服务”。
3. 点击“安装 / 升级插件”。需要自启时勾选“安装时启用开机自动启动”；未勾选会保留已有自启设置。
4. 安装后点击“启动服务”，上方显示流量数据。“查看状态”显示进程与错误日志。

新版仅统计流量，不再检查路由器、香橙派，也不操作 LCD 或雷达。为兼容已有安装，服务包装器仍使用 start_check_monitor 等名称。

离线安装，无需修改 SDK。要求 root ADB、可写 /userdata、Perl 的 JSON::PP / Time::HiRes / Fcntl 模块。测速另需板端 iperf3 和 timeout，当前板子已具备。安装时校验 SHA256 与语法，备份到 /userdata/monitor-backups/随机编号 后替换；失败尝试回滚。来源不明的同名脚本不会覆盖。日志位于 /userdata/log/traffic-monitor.log。

v2.5 开发时已验证插件升级、服务重启与累计保存，并保留原有开机启动设置；跨系统重启通过模拟 boot ID 的板端测试验证。本版的当前状态见前面的“插件中心”说明。

双击 TaishanPiManager.exe 打开独立窗口，无需 Python、浏览器或 WebView。请保留同目录的 adb 和 iperf3 文件夹。

## 新界面

iOS 风格的浅色界面、圆角卡片、蓝色强调色与侧边导航。支持窗口缩放、高 DPI、原生文件选择框；关闭窗口即可退出程序。正在传输或执行命令时会提示等待操作结束。

侧栏底部点击「深色模式」即可切换，再点击「浅色模式」可恢复。六个页面同步切换，Windows 标题栏随应用调整；系统原生文件选择框的外观由 Windows 决定。外观偏好自动保存在当前 Windows 用户设置中，下次启动会恢复；切换不会重连设备或清除输入。

## USB 使用

1. 用可传输数据的 USB 线连接开发板的 OTG / 调试接口。
2. 点击「刷新设备」，选择在线设备。
3. 若未发现设备，请检查数据线、Windows ADB 驱动和板端 adbd。Loader / Maskrom 烧录模式不是 ADB 模式。

网络连接需要设备已开启网络 ADB；点击「网络连接」，填写 IP 和端口。

## Wi-Fi 扫描与连接

1. 通过 USB 连接泰山派，在软件顶部选中设备。
2. 打开左侧「Wi-Fi 网络」，点击「扫描附近 Wi-Fi」。扫描发生在泰山派上；首次使用会自动选择无线网卡，发现多个网卡后可切换。
3. 在列表中选择网络，输入密码，点击「连接 Wi-Fi」。开放网络无需密码。
4. 等待认证和 DHCP 地址分配，约 1 分钟以内。成功后显示网络名称和 IPv4；若仅完成认证，会明确提示尚未获取 IPv4，可点击「刷新状态」。

支持当前 SDK 中 wpa_supplicant / wpa_cli 的 WPA/WPA2 个人网络（包括允许 WPA2 的 WPA2/WPA3 混合网络）和开放网络。隐藏网络、企业 EAP、WEP、纯 WPA3 暂不支持。检测到 NetworkManager、ConnMan 或 iwd 时会提示不兼容，不接管其配置。

扫描显示名称、信号强度、频段和加密类型；同名接入点可重复出现，悬停名称可查看 BSSID，连接由无线服务选择同名网络的接入点。没有网络时显示空列表。若已有 wpa_supplicant 无可用控制接口，会提示修复 ctrl_interface，不会终止已有无线进程。

连接使用新的网络配置；认证失败或保存失败会尝试删除本次新增配置并恢复原连接。认证成功后会调用 wpa_cli save_config 写入泰山派的 wpa_supplicant 配置，并给新网络设置较高优先级，重新上电后由 wpa_supplicant 自动连接。密码不保存到电脑；若板端配置文件不可写，会明确提示保存失败。

密码默认隐藏，点击连接后清空，不保存到电脑或日志；WPA/WPA2 在电脑端派生 PSK 后通过 ADB 标准输入传递，SSID 使用十六进制避免 Shell 解释。派生密钥仅供板端无线服务在内存中使用。建议通过 USB 操作，网络 ADB 在切换 Wi-Fi 时可能断开，需要 USB 或新地址重新连接。

v2.2.1 已实机验证无线扫描、WPA2 密码认证、DHCP 和公网连通性。`Wi-Fi示例-*.png` 使用模拟数据；`实机-*.png` 使用真实设备数据。实际软件不会生成虚拟网络。

## 功能

- 概览：CPU、内存、温度、运行时间、存储与网络。CPU 第一次连接后需要约 5 秒完成两次采样。
- 文件：浏览目录，双击文件夹进入；上传/下载最大 512 MB，上传同名文件前确认。下载失败不会覆盖已有本地文件。暂不支持目录下载、删除或烧录固件。
- 日志：读取最近 250 行内核/系统/网络监控日志，可另存为本地文件。
- 终端：持续 Shell 会话、实时输出、历史命令、补全、中断、ANSI 彩色显示和复制粘贴。
- 维护：安装监控插件、查看状态、启动/停止服务，可选择配置开机自启；确认后重启设备。

页面中的设备状态均来自实际 ADB；软件不会使用模拟数据。“网络流量实机-*.png”为本版实机截图；“Wi-Fi示例-*.png”为标注的模拟展示。

## 源码与构建

`source/desktop.py` 是 Qt Widgets 原生界面，`source/adb_core.py` 是 ADB 通信逻辑。旧网页版本保留在原目录，本目录是全新的桌面版。

开发需要 Python 3.13、PySide6 6.11.2、pyte 0.8.2、wcwidth 0.8.3 和 PyInstaller 6.22.3。安装依赖后：

```powershell
python source/desktop.py
python -m unittest discover -s source -p test_*.py -v
python -m PyInstaller --noconfirm source/build_desktop.spec
```

将 adb 和 iperf3 文件夹放在生成的 EXE 旁。可通过 `TAISHAN_ADB` 环境变量指定其他 ADB 路径。使用附带的打包配置可避免 PATH 中其他软件的 ICU 库覆盖 Windows 系统库；运行环境为 Windows 10/11 64 位。

此版本通过原生窗口测试、离屏视觉检查及 USB 实机插件安装、重装、监控运行和停止验证。详情见“插件验证记录.md”。
