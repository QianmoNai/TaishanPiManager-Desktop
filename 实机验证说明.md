# v2.2.1 实机验证

测试设备：USB ec005875f0f59a5a，TaishanPi，Buildroot 2024.02，Linux 6.1.141。

本次发现并修复了 v2.2 的实机兼容问题：Rockchip adbd 的 exec-out 参数处理与常见 ADB 不同，且不能按预期输入脚本，导致概览空白、命令失败、Wi-Fi 超时。改用非交互 shell 的标准输入，并增加随机结束标记校验远端退出码；兼容 CRLF 状态输出。

已通过：

- 19 项自动化回归测试，覆盖 ADB 返回码、传输中断及换行解析。
- USB 识别、主机名/内核/内存/温度/CPU 连续采样、磁盘和网络信息。
- 设备目录浏览；内核与系统日志读取；命令成功与退出码 7 的失败情况。
- 真实 ADB 上传/下载与 SHA256 验证；通过应用自身异步操作测试中文文件名上传、列表选择、下载及字节一致性。仅创建随机命名的 /tmp 测试文件，测试后已清理。
- wlan0 附近真实 Wi-Fi 扫描，扫描数量随无线环境变化；列表显示、网络选择、密码遮蔽；浅色/深色离屏渲染。
- 用户在最终 v2.2.1 EXE 中输入密码并点击连接后，确认接入 QWRT_53EE5C_2.4G；wpa_state=COMPLETED、WPA2-PSK、IPv4=192.168.1.148。通过应用同一状态接口再次核对结果。
- 默认路由 192.168.1.1；网关 ping 3/3 成功，www.baidu.com 域名解析及公网 ping 2/2 成功，均无丢包。
- 打包 EXE 启动检查通过；真实窗口进程响应正常。用户亲自在窗口完成 Wi-Fi 连接操作。

尚未验证：

- 重启和服务启停未执行。设备没有安装 /userdata/bin/status_check_monitor 等网络监控脚本，该功能已改为明确提示缺失。
- Windows 桌面自动化组件启动失败（找不到 app-server 路径），未完成对已打开 EXE 窗口的鼠标操作测试。上述界面测试运行原生 Qt 控件、实际业务代码及真实 ADB，仅文件选择对话框使用测试路径替代。

截图“实机-*.png”为真实设备数据，不含模拟网络。
