# v2.3 监控插件安装验证

- 复用用户原有 check_orangepi_ld06_incremental.pl；桌面插件仅使用无 LCD、无 LD06 的网络检查模式，通过单独后台循环每 600 秒执行一次。
- 24 项自动化测试通过，覆盖安装确认取消、自启选项、上传后校验失败不执行覆盖、备份与回滚脚本生成、原有 Wi-Fi 与 ADB 功能回归。
- 实机 USB ec005875f0f59a5a：依赖和 perl -c/sh -n 通过；文件 SHA256 验证通过；首次安装、备份重装、启动、重复启动、状态、首轮检查及停止均通过。
- 经 Qt 安装按钮业务流程操作实机（仅确认对话框采用测试确认），安装结果正确；浅色、深色及最小窗口渲染已检查。
- 首轮网络检查：QWRT 路由器、DNS 与外网正常；香橙派 192.168.1.158 的 SSH/Web 当时不可达，结果 ORANGEPI_FAIL。这是监控目标异常，安装本身成功。
- 修复板端缺失 locale 引起的 Perl 警告，启动包装脚本统一使用 C locale，原有日志仍保留。
- 测试后服务为 STOPPED，未启用开机自启。自启脚本做语法检查，未实际重启设备。
- 备份只保存插件脚本，不包含 Wi-Fi 密码；未修改 SDK。
