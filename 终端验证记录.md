# v2.4 原生终端验证

- 使用 Qt 原生绘制、pyte 终端解析和 QProcess ADB PTY；无网页或 WebView。
- 30 项自动化测试通过，涵盖 ANSI 光标与颜色、中文宽度、备用屏幕恢复、历史缓存上限、Ctrl+C/Tab/方向键、多行粘贴取消、分片 PTY 标记以及既有功能回归。
- 真实 USB 设备 ec005875f0f59a5a：建立 /dev/pts/0 会话，确认 cd /tmp 与环境变量跨命令保留；sleep 40 经 Ctrl+C 提前中断；stty size 与窗口尺寸一致。
- 真实会话中中文输入和绿色输出、uname/hostname/pwd 正常；top 可进入并通过 q 返回 Shell；断开和重新连接成功。
- 为当前 Rockchip ADB 调整 Enter 为 LF；板端无 UTF-8 locale，在当前 Bash 会话设置 input-meta、output-meta 和 convert-meta，避免中文被误当作快捷键。未改动设备持久化设置。
- Qt 离屏窗口连接实际设备进行上述测试，并保存“终端实机预览.png”；未通过桌面自动化操作已打包窗口。

- 最终 EXE 离屏启动检查通过：含原生终端的六个页面、随包 ADB、插件资源、浅深色切换以及正常退出（退出码 0）。
