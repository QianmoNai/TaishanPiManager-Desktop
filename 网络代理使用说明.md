# 网络代理 · v2.22

插件中心 → 网络代理 → 安装 / 升级核心 → 导入配置文件 → 启动。

支持 Mihomo/Clash YAML 或 JSON 配置、规则/全局/直连模式、手动策略组节点切换、停止/重启、开机自动启动及卸载。配置导入前需停止服务；校验失败保留原配置。URL provider 由核心按配置下载，文件型 provider 需要提前放入设备插件目录对应的相对路径。订阅地址请先在电脑上下载为完整 Clash 配置再导入。

核心为官方 Mihomo v1.19.31 Linux ARM64，随软件离线提供。安装无需修改 SDK，不包含个人节点、订阅、SSID、密码或密钥。默认不启用自启，未导入配置时不能启动。

HTTP/SOCKS 共用 7890 端口，默认仅监听泰山派 127.0.0.1。需要让电脑或其他局域网设备使用时，在导入配置前勾选“允许局域网设备使用代理”，然后在客户端手动设置泰山派 IP:7890。此选项在下一次导入配置时生效。保留配置中的代理认证设置。

本版实现显式代理，不接管系统路由、TUN、透明代理或 DNS；导入时关闭这些功能并固定代理端口。普通程序只有主动使用该代理地址才会经过代理。控制接口固定为 127.0.0.1:9090，管理连接经临时 ADB 转发并使用随机密钥，不向局域网开放。

核心与配置：`/userdata/network-proxy`。当前配置权限 600，插件目录权限 700。启动入口：`/etc/init.d/S96tspi-proxy`。核心日志在设备的 `service.log`，启动前校验日志在 `validation.log`；不会自动复制进软件包。每次启动轮换一份运行日志。

安装时验证官方压缩包哈希、上传文件哈希、架构和脚本语法。升级前需停止服务；安装事务失败会恢复原文件。卸载会先停止服务，移除开机入口，并将整个插件目录移动至 `/userdata/proxy-backups/removed-*`，配置和日志仍留在设备备份中。

已完成板端安装、配置校验、启动/停止/重启、模式持久化、手动节点切换、自启入口、卸载与重新安装测试。HTTP 和 SOCKS 实际转发测试使用直连测试规则，没有测试私人订阅或付费代理节点。

## v2.24：节点卡片与连通性测试

进入网络代理并启动核心，页面会自动读取策略组和节点。选择策略组后，组内节点会以卡片全部列出；窗口较窄时自动减少列数，可向下滚动查看。蓝色边框表示当前组选择的节点，点击其他卡片即可切换，核心确认成功后更新选中状态。

- 支持按名称或协议搜索、按配置顺序/延迟/名称排序。
- 点击卡片内“测试”只测试该节点，不会切换节点；点击“测试当前组”测试整组，包含被搜索隐藏的节点。
- 显示延迟（毫秒）、未测试、测试中或不可达。测速由泰山派上的 Mihomo 经指定节点访问测速地址，属于 HTTP 请求耗时，并非 ICMP ping。
- 默认地址 https://www.gstatic.com/generate_204，可改为网络中可访问的 HTTP/HTTPS 测试地址。每节点超时 5 秒，每批最多 4 个并行；“取消”或离开页面后不再发起下一批，当前批次允许完成。
- “不可达”仅指本次节点访问该地址失败，不代表该节点对所有网站均不可用；ADB/控制接口异常会中止测试并单独提示。
- REJECT/REJECT-DROP 是拦截策略，不测速；自动选择/故障转移等策略组由核心决定节点，不能在该组内手动切换。可选择 Selector 手动组操作。
- 所选节点仅影响当前策略组；规则模式是否经过该组由配置规则决定。直连模式不会使用所选代理。
- 本次测速结果保留在当前应用会话中，切换设备会清空；核心历史延迟可作为初始显示。将鼠标移到卡片上可查看完整名称及测量时间。
- 安装、导入配置、开机启动等功能位于可展开的“配置与服务管理”。选择框保留禁用鼠标滚轮切换的行为。

本版本为 Windows 管理界面和控制接口更新，无需同步到 WSL SDK，也无需编译泰山派固件。

## v2.25：订阅链接和页面稳定性

在“配置与服务管理”中，可选择“导入本地文件”，也可粘贴 HTTP/HTTPS 订阅配置链接后点击“从链接导入”。请先停止代理。电脑下载完成后，仍由泰山派上的 Mihomo 校验；失败保留原配置。支持 Clash/Mihomo YAML、JSON 配置链接（可跟随 HTTP/HTTPS 重定向），不支持只返回 Base64 节点列表的通用订阅，需向服务商选择 Clash/Mihomo 订阅格式。下载上限 4 MB，网络等待超时 20 秒。

订阅链接不保存到本地或 SDK，成功后输入框清空；没有新增定时更新订阅功能。配置中的 provider 地址仍按配置保存在泰山派中。

测速、切换节点现在原位更新卡片，避免重建卡片和焦点转移引发滚动跳动。批量测速过程中及结束时保留原顺序；需要重新按最新延迟排序时，主动再次选择“延迟从低到高”。忙碌时重复的设备操作不会重复提交；搜索、浏览和取消保持可用。

## v2.26：网站测试与 IP 信息

插件中心的“Wi-Fi 管理”已统一为“网络设置”，入口仍管理 Wi-Fi、USB 网卡和网络路由。

网络代理页面新增“网站测试”和“IP 信息”区域，宽窗口并排显示，小窗口上下排列。

- 网站测试内置 Apple、GitHub、Google、YouTube，支持单站测试、测试全部及添加 HTTP/HTTPS 网站（最多 8 个，自定义网站仅当前会话保留）。
- 网站请求通过临时 ADB 转发进入泰山派 7890 代理端口，遵循当前 Mihomo 模式及规则，并非测试电脑直连网络。HTTPS 校验证书；每次连接/读取等待超时 8 秒，最多跟随 5 次重定向。
- 显示 HTTP 响应状态及耗时；403 等错误标为受限，连接失败或超时单独显示。该测试不执行网页 JavaScript，也不能证明视频播放、登录等完整功能可用。
- IP 信息经相同代理端口查询 HTTPS ipwho.is 服务，显示 IP、国家/地区、位置、服务商、组织、ASN、时区和更新时间。地区信息来自第三方数据库，可能不精确。
- IP 默认隐藏，可点击显示；按需刷新，也可勾选每 300 秒刷新。自动刷新只在页面可见且没有其他操作时执行。
- 规则模式可能为不同网站分配不同出口，IP 信息仅代表 ipwho.is 查询请求的出口。切换节点/模式、停止服务或切换设备后清除旧结果，需要重新测试。
- 未开启代理核心时会提示先启动；未新增代理配置或路由修改。IP 和查询结果不会写入软件包或 SDK。
