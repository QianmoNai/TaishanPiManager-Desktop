# 桌面版 v2.1 验证记录

2026-09-17

- v2.1：检查五个页面的深色原生控件离屏渲染；通过点击按钮验证双向切换、保留命令输入、持久化保存及重新创建应用窗口后的外观恢复。验证使用临时设置文件，不改动用户的外观偏好。
- v2.0 基线的 7 项 Qt 自动化功能测试通过：无设备与导航、状态展示及断开重置、文件/日志/命令确认、后台任务响应及异常恢复、未授权设备不可选、下载失败保护原文件、上传取消不执行操作。
- 五个页面使用原生控件离屏渲染并检查；修复中文字体显示问题与 1000px 窗口宽度下的卡片溢出。
- 修复打包环境中的 Poppler ICU 同名库干扰：Qt 使用 Windows 系统 ICU，构建配置不再混入不兼容版本。
- 关闭窗口前同时检查业务状态和工作线程状态，避免后台任务刚结束时销毁线程池；7 项测试在修复后全部通过。
- 运行时不加载网页、WebView 或 HTTP 服务。主窗口直接调用 ADB 后端。
- v2.0 打包 EXE 离屏启动检查通过：创建原生窗口、初始化五个页面、调用随包 ADB 读取设备列表并正常退出（退出码 0）。
- 开发板未连接；真实硬件数据、传输、服务启停和重启尚未实机验证。
- `界面预览.png` 和 `界面预览-深色.png` 均为未连接设备的真实界面截图。

- v2.1 最终 EXE 离屏启动检查通过：原生窗口和五个页面初始化、随包 ADB 检查、浅深色双向切换、正常退出（退出码 0）。
